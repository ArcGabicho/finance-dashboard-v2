version: '3.8'
services:
	frontend:
		build: .
		ports:
			- "5173:5173"
		volumes:
			- .:/app
			- /app/node_modules
		environment:
			- CHOKIDAR_USEPOLLING=true # Para hot reload en algunos sistemas
		command: npm run dev -- --host
